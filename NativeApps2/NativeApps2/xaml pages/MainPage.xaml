<Page
    x:Class="NativeApps2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeApps2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Frame x:Name="mainFrame">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="center" Text="Welkom"/>
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Vertical">
                <Button Click="Button_Click" HorizontalAlignment="Center" Background="Blue" Content="Start"/>
                <TextBlock Tapped="login" Text="Registreer of log in"/>
                <TextBlock Tapped="zakelijk" Text="Ik ben een zakelijke gebruiker"/>
            </StackPanel>

        </Grid>
    </Frame>
</Page>
